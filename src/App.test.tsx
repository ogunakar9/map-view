// import { render, screen } from "@testing-library/react";
// import App from "./App";
// import "leaflet/dist/leaflet.css";
// import "leaflet-defaulticon-compatibility/dist/leaflet-defaulticon-compatibility.webpack.css";
// import "leaflet-defaulticon-compatibility";

//TODO: workarounds for react-leaflet package and jest
// 1. Test if the map is rendered on the page
// 2. Test if markers are rendered on the map
// 3. Test if popups are rendered on the map
//  3.1. Test if popups are rendered on the map when a marker is clicked
//  3.2. Test if popups are closed when close icon is clicked

// test("renders learn react link", () => {
//   render(<App />);
//   const linkElement = screen.getByText(/learn react/i);
//   expect(linkElement).toBeInTheDocument();
// });

// test("Markers are rendered on the map", async () => {
//   render(<App />);

//   // Assume Marker labels are present in popups
//   const marker = await screen.findByAltText("Marker");
//   // const marker1 = s.getElementsByClassName("leaflet-popup-content");
//   expect(marker).toBeInTheDocument();
// });
